# Final human actions

The code/evidence package is **READY WITH LIMITATIONS**. Complete these actions before pressing Submit.

## Required before submission

1. Review the six audit-changed files and confirm the independent **87/100** score and limitations are acceptable. Do not restore the old 91/100 as an “independent” score; it remains a clearly labelled self-assessment in the historical checklist.
2. Commit and push the privacy correction plus the four final-audit artifacts. Confirm the public branch contains the intended commit and has no untracked local files, `.venv`, `.env`, credentials, or temporary audit outputs.
3. Record `docs/DEMO_SCRIPT.md` at no more than 4:50. The written target leaves only 10 seconds of competition margin, so do not add unscripted detail that pushes the final media above five minutes.
4. Upload the video and set the exact access mode required by the competition. Add the final video URL to the portal or submission material; no video URL is currently verified in the repository.
5. In a logged-out/incognito browser, open the repository URL and video URL from start to finish. Confirm the default branch, README, reports, trajectories, raw results, and video are accessible without requesting permission.
6. Complete the submission portal inventory using `docs/SUBMISSION_CHECKLIST.md`. Save a receipt or screenshot only after the portal confirms submission. Portal completion is currently unverified.

## Presenter checks

- Open with the named payment-operations user and fragmented-evidence bottleneck.
- Demonstrate `conflicting_states`, including `FAILED` versus `POSTED`, 0.62 uncertainty, verifier feedback, and no retry/reversal recommendation.
- State that the fair baseline correction changed 11/13 to 12/13 and that the prompt-injection failure remains visible.
- State that final 13/13 and perfect secondary scores are from 13 same-authored synthetic cases, with only one contradiction-eligible case.
- Describe runtime as local deterministic processing, not operator time saved or production capacity.
- Say **DEFENSIBLY AGENTIC** if asked; acknowledge fixed planning/tool selection and avoid claiming autonomous general intelligence.
- Preserve the hot take: deleting unsupported alternatives improved trust; do not add an unnecessary LLM for presentation.

## Highest-value optional improvement

If the deadline permits independent coordination, freeze a separately authored blind holdout before the implementation sees its labels. Include at least five conflict cases, semantic citation-laundering, prompt-injection variants, novel fields/schema drift, empty-claim scorer checks, and ambiguous downstream outcomes. Report every failure. This is the single most credible way to recover score.

## Claims to avoid

Do not claim:

- production readiness or validated production connectors;
- measured investigation-time reduction or customer outcome improvement;
- generalization beyond the 11 declared classes;
- that 100% on this set implies zero real-world error;
- that approval labels replace organizational authorization;
- that TracePay outperforms unseen competitors or “millions of agents”;
- that the video or portal is complete until directly verified.

## Final go/no-go

- Code, safety, metrics, clean reproduction, and deliverable inventory: **GO**.
- Repository push, recorded video, link permissions, and portal receipt: **PENDING HUMAN VERIFICATION**.
- Recommendation until those are complete: **FIX FIRST**.
